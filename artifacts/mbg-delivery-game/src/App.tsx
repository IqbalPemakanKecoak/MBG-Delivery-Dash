/**
 * App.tsx
 * Root component — renders the MBG Delivery Game directly.
 * No routing needed; the game is the full page.
 */
import { Game } from './game/Game';

export default function App() {
  return <Game />;
}
